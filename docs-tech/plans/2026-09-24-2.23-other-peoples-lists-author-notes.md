# 2.23 plan — the authors' notes

Evidence behind the tasks in [the plan](2026-09-24-2.23-other-peoples-lists-implementation.md): sources relied on, decisions the spec did not make, commands run, and what could not be verified. The pre-flight reviews are summarised at the end.

## Task 1

## Task 1 — author notes (T1)

Branch `T1` in `$S/wt-T1`, based on `44f3641` (= real `4448709`). Commits:
`f2b7df6` P1 · `a2f4970` the script's output · `db30107` the migration.

### Sources relied on

| Fact | Source |
|---|---|
| P1: the test is red at the branch head, on research.md:785 | `go test ./internal/shared -run TestNoPersonalEmailAddressesAreTracked` @ 44f3641 → FAIL `Hagezi's support address` |
| `autocert.HostWhitelist` is upstream | `internal/web/acme.go:59` @ 44f3641 |
| 2247 occurrences / 148 files in the whole tree; 1137 / 112 outside history | `git grep -oiE 'blacklist\|whitelist'` @ f2b7df6, with and without the P2 exclusions |
| No uppercase `BLACKLIST`/`WHITELIST` outside specs | `git grep -nE 'BLACKLIST\|WHITELIST'` @ 44f3641 → research.md:523 only |
| No `de`/`fr` value contains either word | per-value grep of `locales/{de,fr}.json` @ 44f3641 |
| The export format is `shared.Rules` marshalled | `internal/core/rules.go` `ExportStaged` → `json.MarshalIndent(state.Staged, "", "  ")`; import: `ImportRules`, `daemon.go` CmdImportRules, `democlient.go` `handleImportRules` all `json.Unmarshal` into `shared.Rules` |
| `rules.json` holds three copies, rewritten whole on every save | `rules.go` `save` → `json.MarshalIndent(state)` |
| TOML is written back from the struct | `internal/core/config.go` `saveLocked` → `toml.NewEncoder(tmp).Encode(snapshot)` |
| `MetaData.IsDefined` | `github.com/BurntSushi/toml@v1.6.0/meta.go:28` (go.mod pins v1.6.0) |
| Legacy-field precedent | `IPv6Config.Enabled` models.go:365, `migrateIPv6Mode` config.go, explained in `diff.go:313` |
| No env var names a list | `internal/shared/env.go` `CoreEnvVars`: socket_path, data_dir, log_dir only |
| `applied-config.json` carries Go field names | `FirewallOptions` has only `toml` tags (models.go); `writeAppliedConfig` marshals `shared.AppliedConfig`; v2.22.0 output: `"LogBlacklist": true` |
| Boot rewrites it | `internal/core/restore.go:204` `f.recordAppliedConfig` |
| packets.log is rewritten at start | `internal/core/packetlog.go` `Persist` → `s.rewrite(p.oldestFirst())` |
| 301 may turn POST into GET | RFC 9110 §15.4.2 (rfc-editor.org/rfc/rfc9110.txt lines 7384-7387, fetched 2026-09-24) |
| The gate walk floor is 41; the walk counts 51 after this task | `middleware_test.go` `if checked < 41`; measured with a temporary `t.Logf` |
| `TestEveryPageIsDocumented` parses `r.Get("…")` from server.go | `internal/shared/docs_coverage_test.go` regex `\br\.(?:Get\|Handle)\("([^"]+)"` |
| jekyll-redirect-from is a plugin already | `docs/_config.yml:70`, `docs/Gemfile:6` |
| `*.log` is gitignored | `.gitignore:54` — the fixture is `packets.jsonl` |
| `check:changelog` needs a bold tagline under each heading | its own failure message on the first attempt |
| Fixtures | produced by `v2.22.0` (`445714a`) code via `git archive v2.22.0` + `TestZZWriteFixtures` (plan Step 3) |

### Decisions not in the spec

- The script writes no legacy literal; all old-name readers are hand-written after it. So the script's exclusion list is only history + `HostWhitelist` + binaries/SVGs.
- `docs/features/blacklist.md` keeps its path (it serves the old URL `/features/blacklist/`); the brief suggested `git mv` for these stubs — not done, deliberately.
- A `rules.json`/export naming a list both ways is **refused**; a TOML with both log keys: **new wins** + warning.
- `applied-config.json` migration added — not in spec §6's table; found by reading `appliedconfig.go`.
- POST to an old route is **served**, not redirected.
- `/blocked/stage act=` also mapped (not asked; one call).
- Audit log is not rewritten.
- `TestTheGateCannotBeWalkedPast`'s floor (41) is left alone: it is a floor and still holds (51 walked after this task, four of them its new routes); raising it is not this task's business.
- The guard counts **exactly** per file (not a ceiling) and also checks file names.
- `CHANGELOG.md` gets `## [Unreleased]` with the tagline `**Other people's lists.**` (check:changelog requires one); Task 8 owns the rest of the section.

### Commands run, and results

| Command | Result |
|---|---|
| P1 test before / after the sed | FAIL / ok |
| `go test ./internal/... ./cmd/...` @ f2b7df6 | ok ×4 |
| `python3 rename-2.23.py` | renamed 11, rewrote 108 |
| `go build/vet/test` right after the script | all ok (before any hand work) |
| `npm run build:diagrams` then restore 8 unchanged | `check:diagrams`: 9 current |
| occurrences after the script | 3 (HostWhitelist) |
| stub `CurrentListName` + new tests on a2f4970 | 16 of 19 new tests FAIL; the 3 passing guard the new path (mutations M3, M16, M17) |
| `go test ./internal/... ./cmd/...` at the end | ok |
| `~/go/bin/golangci-lint run ./...` | 0 issues |
| `npm run build:css`, `build:docs-css` | byte-identical to the pre-rename tree (58 780 bytes) |
| `npm run check:changelog` | 42/42/42 agreed |
| `npm run check:prose` pre vs post | exit 0 both; same findings, renamed paths only |
| Jekyll build (the `scripts/docs-build.sh` podman line, image already present) | `/docs/features/{blacklist,whitelist}/` and `/features/blacklist/` redirect to the new pages |
| mutation table (runner `$S/t1-mutate.py`, in a throwaway worktree; M17, M18 by hand) | 18 mutations, all compile, each reddens its tests and nothing else; plan Step 7 |
| plan Step 3 generator re-run from the plan text | reproduces all five fixtures (rules.json/export.json differ only in the random rule id) |
| plan code blocks vs branch | the script and all five new Go files byte-identical; every fragment found verbatim in its file |
| `go vet -tags integration ./...`; core integration binary under `unshare -rn` | ok; 626 PASS, 10 SKIP, 0 FAIL (incl. every renamed `TestIntegration_*Blocklist*`/`*Allowlist*`) |
| `check:ui` on 12281 (`flock`, demo dir `~/.local/share/easywall-demo-t1`) | 39 ok, 0 FAIL, "UI checks passed" |

### Not verified

- `check:docs` (browser check of the docs site; needs the pagefind index) — not run.
- Integration tests needing real root (none of this task's) — not run under `sudo`.
- `internal/web` integration tests (`acme_integration_test.go`): compiled by `go vet -tags integration`, not executed.
- The Chrome render check (controller's).
- The guard lesson: while `old_list_names_test.go` was untracked it passed and ignored itself; the first mutation run was therefore red on the guard for every mutation. Fixed (the file lists itself, 6) and the run repeated; the table in the plan is the second run.
- `debian/control` wording ("IP blocklists and allowlists") not built into a package.
- Whether any operator tooling outside this repository parses `rule_type` from the audit log.

### FIX-A pass (R1)

| Finding | Change | Commit (T1) |
|---|---|---|
| R1#5 | CHANGELOG bullet: export before upgrading if you might go back — 2.22 reads a 2.23 `rules.json` as empty lists and a 2.23-saved `easywall.toml` as the blocklist log off; `docs/_docs/changelog.md` rebuilt (`check:changelog` agreed) | 6735d7d |
| R1#6 | `readOldLogKeys` comment and the invariants row: the file is generated once from a template and never replaced, not a conffile | 6735d7d |
| R1#7 | `handleOptionsPOST` → `currentFieldNames(r.Form)`: a field posted under a pre-2.23 name (split on `_`, each part through `CurrentListName`) is read as the new one unless the new one is also posted. No old word in `handler_options.go`. `TestAnOldOptionsTabKeepsTheBlocklistLog` (guard count for `renamed_routes_test.go` 11 → 14). Mutations M19 (call removed), M20 (old overwrites new) → red | 6735d7d |

Plan updated in place: Files list, the `readOldLogKeys` block, a `handler_options.go` block, the created `renamed_routes_test.go` block (byte-equal to the branch again), the guard count, M19/M20, the invariants row, the CHANGELOG block, the commit step. Stub count in Step 4: 17 of 20 new tests fail. Unit tests, lint, prose ok at 6735d7d.

## Task 2

## Task 2 — author notes (T2)

Branch `T2` in `$S/wt-T2`, **rebased onto `T1` (`6735d7d`)** by FIX-A; it was written on `a18d4bd` (the mechanical-rename stand-in). Pre-rebase head kept as branch `T2-pre-rebase` (`57cce62`).

### Sources relied on

| Fact | Source |
|---|---|
| Kernel log prefix limit: `NF_LOG_PREFIXLEN 128` | `include/uapi/linux/netfilter/nf_log.h:13` @ v6.12 (`gh api repos/torvalds/linux/contents/…?ref=v6.12`) |
| `NFTA_LOG_PREFIX` is `NLA_STRING`, `.len = NF_LOG_PREFIXLEN - 1` | `net/netfilter/nft_log.c:123-124` @ v6.12 |
| `NLA_STRING` length check drops one trailing NUL, then `attrlen > pt->len` → error, so the maximum is 127 bytes of text | `lib/nlattr.c:483-495` @ v6.12 |
| Spamhaus homepage `https://www.spamhaus.org/blocklists/do-not-route-or-peer/`, terms `https://www.spamhaus.org/drop/terms/` | research.md:78 |
| DShield: only human page cited is `https://isc.sans.edu/api/` (terms quoted there) → used for both | research.md:82 |
| blocklist.de: `https://www.blocklist.de/en/export.html` ("at your own risk") → both | research.md:83 |
| CINS: `https://cinsscore.com/` ("use in any way you see fit") → both | research.md:84 |
| ET: homepage `https://rules.emergingthreats.net/OPEN_download_instructions.html`, terms `https://rules.emergingthreats.net/open/suricata-7.0.3/LICENSE` | research.md:81 |
| IPsum: `https://github.com/stamparm/ipsum` (Unlicense in the repo) → both | research.md:86 |
| Hagezi: research cites the repo as `hagezi/dns-blocklists@main` (research.md:692) and its GPL-3.0 licence (:802) but gives **no literal URL**; `https://github.com/hagezi/dns-blocklists` is derived from that repo name → both | research.md:692, 802 |
| Tor: CC0 stated on `https://metrics.torproject.org/collector.html` → both; `check.torproject.org/torbulkexitlist` is the data URL | research.md:87 |
| Data URLs, formats, intervals, verdicts, FP levels | spec §1 tables |
| Spamhaus v6 URL `https://www.spamhaus.org/drop/drop_v6.json` | spec §1 (`…/drop_v6.json`), research.md:40 |
| Spamhaus DROP size 1710 + 91 (demo figure 1801) | spec §1 |
| `TestDemo_AnswersEveryDeclaredCommand` hard-codes the count | `internal/web/democlient_test.go:617` @ a18d4bd |
| `TestLogPrefixesMapToTheRulesTheyName` compares its map length to `len(PacketLogRules)` | `internal/core/nftables_log_test.go:135` @ a18d4bd |
| `TestEveryBlockedRuleLeadsToItsOption` refuses an empty option for any rule but `drop`/`blocklist` | `internal/web/handler_blocked_why_test.go:181-185` @ a18d4bd |
| `TestEveryPreviewSetHasALabel` pins `len(previewSetOrder)` to `Rules`' field count | `internal/web/reach_labels_test.go:81` |
| `TestEveryCommandIsDocumentedInBothPublishedAndTechnicalDocs` reads `docs/_docs/architecture.md` **and** `docs-tech/protocol.md` | `internal/shared/docs_coverage_test.go:314-329` |
| `check:ui` binds **12228** for its gate sub-check (not only 12229–12233) | `scripts/ui-check.mjs:603,618,623,648` @ a18d4bd |

### Decisions not in the spec

| # | Decision | Why |
|---|---|---|
| T2-a | `SendCommand` refuses a marshalled command over `MaxMessageBytes` **before dialling**, returning an error wrapping the new `shared.ErrRequestTooLarge` (`errors.New(ErrRequestTooLargeText)`) | measured need: the core stops reading at limit+1 and closes; the sender's remaining write then fails with EPIPE and never sees the core's answer. P12's "measure before sending" becomes one check in the transport every caller shares. **Addition to interfaces.md** (`ErrRequestTooLarge`) |
| T2-b | The core does not drain an over-long request | draining an unbounded stream from a socket with no read deadline could hold the goroutine for ever; the sender-side refusal covers the real client |
| T2-c | Dispatch: `case shared.CmdUpdateFeed, shared.CmdGetFeeds:` answers `"<CMD> is not implemented yet"` | `TestDaemonDispatch_HandlesEveryDeclaredCommand` fails on "unknown command"; leaving dispatch to Task 4 ships a red tree. Task 4 replaces the case |
| T2-d | `blockedRuleOption["feed"] = "log_feed_connections"` | per the brief; the guard refuses `""` for any rule but `drop`/`blocklist`. Task 7 may prefer the blocklist page's feed card and must then change the guard too |
| T2-e | `previewSetOrder` gains `"feeds"` between `allowlist` and `forwarding`; locale `apply_set_feeds` | the chain order (spec §4) |
| T2-f | `DiffRules` labels a feed by `FeedDisplayName` ("Spamhaus DROP", "Own feed 2"); an unknown id keeps no label | English in `shared`, like `portDetail`'s "from"; new helper `FeedDisplayName` — **addition to interfaces.md** |
| T2-g | `DescribeRuleChange("feeds", …)` → `added spamhaus-drop, removed dshield` | the audit line for a `rules_saved` of type `feeds`; otherwise it was `""` |
| T2-h | `ParseLogPrefix` is wired into the core's `entryFromAttribute` now (`e.Rule, e.Feed = …`) | one line, and it makes `PacketLogEntry.Feed` real before Task 3 writes the rules |
| T2-i | `AllFeedVerdicts`, `AllFeedFalsePositives` lists; label keys `feed_verdict_<v>`, `feed_fp_<level>`, `feed_link_source`, `feed_link_terms` | the locale guard needs closed lists, as `AllSuggestions` is for the ports catalogue. **Additions to interfaces.md** |
| T2-j | Catalogue `Name` stays a Go string, not a locale key | spec §1 lists Name as a catalogue field; four of the eight are proper names. Task 7 decides whether "Tor exit nodes" etc. need a `de` rendering |
| T2-k | German: "Nur wenn man weiß, warum" — impersonal, because `de.json` mixes *Sie* and *du* | — |
| T2-l | Demo seeds `Rules.Feeds = ["spamhaus-drop","dshield"]` in all three states, `LogFeed: true` | Task 7's screenshots need an enabled feed with a copy; switching them on in Current means `/apply` shows no feed diff by default |
| T2-m | Web test fake core (`testhelpers_test.go`) reads `MaxMessageBytes+1` | Task 6's tests send large `UPDATE_FEED`s; at 1 MiB they were silently truncated |
| T2-o | `TestHandleOptionsPOST_ReadsEveryOption` (new, reflection) | the first mutation run found that dropping `LogFeed` from `handleOptionsPOST` reddened nothing — no test read the form handler field by field |
| T2-p | `echoServer` split into `rawServer`; cleanup order swapped | a mutation (`>= MaxMessageBytes`) made the client never dial, and the old order (wait on `Accept`, then close) hung the package for the 10-minute test timeout |
| T2-q | **8 MiB** (coordinator's ruling after the measurement) | 100 000 × longest IPv6 bare = 4 200 059 bytes, ×`/128` = **4 600 059** (the first draft said 4 500 059 — that was a 38-character address; the test caught it), IPv4 /31 = 2 100 059. Margin at the /128 worst case: 3 788 549 of 8 388 608. `TestMaxMessageBytesCarriesAFullFeed` pins the three sizes exactly and that each fits |
| T2-n | `LogFeedLimit` is per rule — one rule per feed and family — and the copy says "per feed and address family" | the existing log rules carry an anonymous per-rule limit (`logExprs`, nftables.go:990). **Task 3 must build it that way or change the three strings** (`opt_log_feed_connections_breaks` ×2, configuration.md row, schema description) |

### Commands run, and results

| Command | Result |
|---|---|
| `go build ./... && go vet ./...` | clean |
| `go test ./internal/... ./cmd/...` | ok (core, shared, web, cmd/easywall-core) |
| `go vet -tags integration ./...` | clean. **Missed at first**: the untagged vet/grep never saw `nftables_check_integration_test.go`, whose `firewallOptionBools = 22` went red with `LogFeed`; the coordinator caught it, fixed in `9d83a14` (22 → 23), and breaking it back to 22 turns `TestIntegration_TheBuiltTableHasNoFindings` red |
| `go test -c -tags integration -o $S/T2-core.test ./internal/core/ && (cd internal/core && unshare -rn $S/T2-core.test -test.count=1)` | **PASS**, 621 tests, 0 skipped |
| same for `./internal/web/` (the only other `//go:build integration` package) under `unshare -rn` | one failure: `TestIntegration_ACertificateArrivesOverHTTP01` — `podman run …: exit status 125, mkdir /run/libpod: permission denied`. **Identical at `a18d4bd`** (built and run there), so an environment limit, not this branch. Without `unshare` the binary's TestMain re-execs into `CLONE_NEWNET` and exits 1 silently for lack of CAP_SYS_ADMIN. `sudo` for that one test did not return within 300 s (password prompt or image pull; not diagnosed) |
| `~/go/bin/golangci-lint run ./...` | 0 issues |
| `node scripts/prose-check.mjs` | exit 0; 0 sentences over 30 words |
| `codespell` (repo config) | exit 0 |
| `EASYWALL_DEMO_DIR=~/.local/share/easywall-demo-t2 EASYWALL_DEMO_ADDR=127.0.0.1:12282 scripts/demo-server.sh` + `flock $S/checkui.lock npm run check:ui` | **UI checks passed** — 15 pages clean at 1600/1440/1200/900/390 in both themes, German fits, `/options` disclosure fits at 390 px in German. Playwright only; the Chrome check is the controller's |
| `go run` of a size program (100 000 entries per spelling, marshalled as `Command{UPDATE_FEED}`) | v4 /31 2 100 059; v4 /32 bare 1 800 059; v6 38-char 4 100 059; v6 39-char 4 200 059; v6 38-char /128 4 500 059; v6 39-char /128 4 600 059 (the last two pinned in the test with the 39-char bare) |
| plan re-applied to a fresh `a18d4bd` worktree (new files from the `Create` blocks, every `diff` block through `git apply`) | applies cleanly; `git diff --cached T2` empty — the plan is the branch. Step 1 alone → `go vet` gives the three errors quoted in Step 2 |
| mutation batches (8 MiB change: `$S/t2-muts4.json`, all four red) `$S/t2-muts1.json`, `t2-muts2.json`, `t2-muts2b.json`, `t2-muts2c.json`, `t2-muts3.json`, `t2-muts3b.json` via `$S/t2-mut.py` | every mutation reddens at least one test — see the plan's mutation table; `git status --short` clean after each batch |
| `gh api …nf_log.h / nft_log.c / nlattr.c ?ref=v6.12` | see Sources |

### Not verified

- `TestIntegration_ACertificateArrivesOverHTTP01` did not run here (podman in a user namespace); CI runs it with `sudo`.
- The two feed commands are **not** exercised against a real core: dispatch answers "not implemented yet" until Task 4.
- `FeedLogPrefix` against a real kernel: the 127-byte bound is read from source, not loaded (Task 3 loads the rule).
- The Homepage/Terms URLs were **not fetched** (brief: the research is the evidence). The Hagezi URL is derived from a repo name, not quoted.
- The new `/options` card was rendered only by `check:ui` (Playwright), not in Chrome.
- `check:ui` uses port 12228 for its gate sub-check (`scripts/ui-check.mjs:603`); the coordinator has corrected the plan's Global Constraints.

### FIX-A pass (R1, rulings X5, X6)

| Finding | Change | Commit (T2) |
|---|---|---|
| X6 / R1#3 | `git rebase --onto T1 a18d4bd T2`; one conflict (`docs-tech/invariants.md`, both new sections kept); `ParseLogPrefix` keeps T1's `CurrentListName` line; every diff block regenerated as `git diff T1 T2`; the plan re-applied to a fresh `T1` checkout gives `git diff --cached T2` = empty | rebase |
| X5 / R1#1 | `LoadConfig` decodes with `toml.Decode`; absent `FirewallLimits` keys get `Default`, before `readOldLogKeys`; `TestA222ConfigGetsTheNewLimitsDefault`. Mutations: `&& false` → red; `|| true` → `TestAnOutOfRangeLimitInTheFileIsClampedAndSaidOutLoud` red; loop moved after `readOldLogKeys` → `TestA222ConfigGetsTheNewLimitsDefault` + `TestA222ConfigReadsTheOldLogKeys` red. R1's proposed placement ("after the old-key read") would have overwritten the old limit — measured by that last mutation | e4f204b |
| R1#8 | `Rules.Feeds` comment | e4f204b |
| R1#9 | invariants: a catalogue id is retired, never deleted | e4f204b |
| R1#10 | CLAUDE.md "twenty-five commands" | e4f204b |
| coordinator's 57cce62 | `ErrFeedShrankText` rebased as `8794de2`; in the plan's Interfaces, Step 3 note and mutation table (70→60 → `TestErrFeedShrankTextNamesThePercent` red, re-measured) | 8794de2 |
| integration, rebased | core rootless: 630 pass, 10 skip (read-only-dir tests as uid 0), 0 fail; web: only `TestIntegration_ACertificateArrivesOverHTTP01` (podman in userns), same as before | — |

## Tasks 3–5

## Tasks 3–5 — author notes (CORE)

Branch `CORE` in `$S/wt-CORE`: `ee4a5a4` (T3) → `f07f5b8` (T4) → `970df30` (T5), on T2 `e4f204b` (rebased onto Task 1 `6735d7d`).
Plan replayed onto a fresh `e4f204b` worktree (Create blocks written, diff blocks through `git apply`): `git diff --cached 970df30` empty.

### Sources relied on

| Fact | Source |
|---|---|
| A batch is one `sendmsg` | `mdlayher/netlink@v1.11.2/conn_linux.go:106-119` |
| `WithSockOptions` runs on every dial of a non-lasting conn | `google/nftables@v0.3.0/conn.go:111-117, 299-322` |
| `SetAddElements` = one `NFTA_SET_ELEM_LIST_ELEMENTS` per call; element marshalling | `set.go:374-491` @ v0.3.0 |
| `FlushSet` = `DELSETELEM` with no elements | `set.go:724-743` |
| `AddTable` uses `NLM_F_CREATE` without `EXCL` (idempotent) | `table.go:69-97` |
| rbtree overlap → `EEXIST`/`ENOTEMPTY`; `ENOTEMPTY` rewritten to `EEXIST`; adjacency accepted | `net/netfilter/nft_set_rbtree.c:422-461`, `nf_tables_api.c:7192-7196` @ v6.12 |
| CI runs integration as root | `.github/workflows/test.yml:143` |
| Health and usage skip any `_` id | `health.go:262-272`, `usage.go:176` |
| No production "decide" caller | `grep -rn decide internal/core` (none) |
| `net.core.wmem_max` here 4 194 304, `wmem_default` 212 992; kernel 7.2.3 | `sysctl`, `uname -r` |

### Decisions not in the spec (full tables in plan-CORE.md)

C1 atomic rebuild (the window grew to 525 ms; fixed by one transaction, not only by moving elements) · C3 merge adjacency anyway · C4 open-ended top range · C6 P3's "rootless cannot" is host-dependent · C8 feed-store errors never fail a write · C9 no fourth caller · D4-1 **no counter booking before a refresh (spec step 8 says book)** · D4-2 no post-write panic check · D4-4 breadth before drop · D4-5 empty result refused · D4-6 IPv6 global = 2000::/3 · D4-8 audit only changes and data refusals · D4-9 reload a live feed on every update · D4-10 slot held ≈0.3 s per refresh (can bounce an APPLY/RESUME) · D5-2 new reason `feeds_unreadable` (addition to interfaces.md) · Task 4 touches `internal/web/server.go` for the two audit labels.

Additions to interfaces.md: `FeedStore.Get/Ranges/Contains/AllowlistOverlap`, `(m) addLogged`, `(m) flushLarge`, `shared.ReasonFeedsUnreadable`, `applyVerdict.Feed`.

### Commands run, and results

| Command | Result |
|---|---|
| `go build ./... && go vet ./... && go vet -tags integration ./internal/... && go test ./internal/... ./cmd/...` at each commit | ok |
| `~/go/bin/golangci-lint run ./...` | 0 issues |
| full integration suite `unshare -rn` at T3, T4, T5 | PASS (T3 first run red only on T2's `firewallOptionBools`, fixed by T2 in `9d83a14`) |
| window, before C1 (5 runs) | best/worst: 1.08/1.86 ms · 325/350 ms · 476/525 ms |
| window, after C1 | observer: 0 misses (9 / 3 651 / 5 737 looks); with the old separate flush: 13/31, 5 085/5 150, 7 101/7 207 |
| refresh atomicity | 0 of 4 562 misses; split flush 4 456 of 4 599 |
| 3 × 100 000 rootless | `EMSGSIZE … need a 12261376-byte send buffer … net.core.wmem_max` |
| mutation batches `core-muts3*.json`, `core-muts4*.json`, `core-muts5*.json` via `$S/core-mut.py` | every mutation red except M3, N28 (dead code, deleted); `git status` clean |
| `node scripts/prose-check.mjs` | exit 0 |
| Task 5 cherry-picked alone onto `fab3757` | applies; shared + web green; integration vet ok |

### Not verified

- **Nothing ran as real root.** `sudo` waited for a password nobody typed (120 s timeout). So the `SO_SNDBUFFORCE` success path, and the 100 000-element tests on a stock `wmem_max`, are unrun; on this host they passed rootless only because `wmem_max` is 4 MiB. CI (sudo) will be their first root run.
- Kernel v6.12 behaviour read from source; measured only on 7.2.3.
- Catalogue sizes' IPv4/IPv6 split: all IPv4 except Spamhaus's 91 v6 (not measured); ≈150 000, not the brief's ≈200 000.
- `check:ui` not run: the only template change is one `dict` argument; `TestHandleApplyGET_TheVerdictNamesTheFeed` renders it. No Chrome/Playwright check of `/apply` with the new sentence, either language.
- De wording (`reach_in_feed`, audit labels) not reviewed by a native speaker.
- Race of D4-10 (refresh holding the slot while RESUME arrives) reasoned, not reproduced.

### FIX-B pass (review R2, rulings X1–X4)

| Finding | Where | Evidence |
|---|---|---|
| R2#1 / X1 shrink only while live, `ErrFeedShrankText` | T4 | `TestUpdateFeed_AShrunkFeedIsAcceptedAfterOffApplyOn`, `TestIntegration_AShrunkFeedLoadsAfterOffApplyOn`; mutations N40, N41, N46 |
| R2#2 / X2 load first, audit a kernel refusal | T4 | refusal-table row; N42, N43 |
| R2#3 / X3 `CapAcknowledge` | T3 | `TestIntegration_ARefusedLargeBatchSaysWhy` → ENOENT; M40 → ENOBUFS |
| R2#4 / X4 no-copy feed → unknown | T5 | `TestReachVerdict_AFeedWithNoCopyYetIsUnknown`; R13–R15 |
| R2#5 baselines after a refused rollback | T3 | `errNothingWritten`; `TestIntegration_ARefusedRollbackKeepsTheBaselines`; M41, M42 |
| R2#6 stale comment, one pair of figures (best of five 325/476) | T3 | code, invariants, plan C1 |
| R2#7 refused batch keeps the old table | T3 | `TestIntegration_ARefusedBatchLeavesThePreviousTable`; M43 |
| R2#8 teardown modelled as `Reset()` | T4 | test rewritten |
| R2#9 factor two | plan C6 | `sock.c:1318-1326`, `af_netlink.c:1858` |
| R2#10 skip on a capped host unless `EASYWALL_REQUIRE_SELFTEST` | T3 | `skipOrFailUnprovable` in two tests; Task 8's command list is the controller's |
| R2#11 measured 2.7 MB / 14 ms per write → skip checked-only writes | T4 | N44, N45 |
| R2#12 comment | T3 | `addFeeds` |

Full rootless integration suite at `970df30`: PASS. Still nothing run as real root.

## Task 6

## Task 6 — author notes (WEB)

Branch `WEB` in `$S/wt-WEB`, one commit on `T2` @ `e4f204b` (T2 rebased onto Task 1's real tree; WEB re-applied onto it as a patch, then one squashed commit).

### Sources relied on

| Fact | Source |
|---|---|
| The transport requests gzip and decompresses only when the caller leaves `Accept-Encoding` empty (and no Range, not HEAD) | `$(go env GOROOT)/src/net/http/transport.go:2998-3016` @ go1.27.1 |
| HTTP/2 does the same: `IsRequestGzip(…)`, then wraps the body in `gzipReader` when the response says gzip | `src/net/http/internal/http2/transport.go:1121, 2296-2300` @ go1.27.1 |
| Therefore a `LimitReader` on `resp.Body` caps the **decompressed** size; setting the header by hand hands back the compressed stream | mutation M14 (both gzip tests red) |
| `http.Client.Timeout` covers the body read, and its error is a `net.Error` with `Timeout()` | `TestFeedRequestIsBoundedInTime` (headers sent, body never ends → `timeout`) |
| ET: at most one download an hour, 30-minute cool-down | research.md:53, :81 |
| DShield: back off 5 min on 429; custom UA with contact | research.md:55, :82 |
| Spamhaus: automated downloads ≥ 1 h apart; skip the `type: metadata` line | research.md:78, :158 |
| CrowdSec: 1 pull per 24 h, 429 above; silent truncation above ~5 MB uncompressed → send gzip; optional CIDR aggregation | research.md:222, :267, :269, :270 |
| cs-blocklist-mirror listens on `127.0.0.1:41412` (P14's loopback http) | research.md:380 |
| dan.me.uk answers 200 with an apology | research.md:29, :196; the body itself: research scratchpad `feeds/danme` |
| GitHub raw honours `If-None-Match`, not `If-Modified-Since` | research.md:700 |
| Hagezi TIF carries 100.83.3.144 and three 240/4 addresses | research.md:808 (§pihole 4); counted: 4 of 79 175 in `feeds2/tif` |
| The outbound pattern: zero Transport → `ProxyFromEnvironment`; redirects refused; `#nosec G704` | `internal/web/notify.go:69-80, :97` @ T2 |
| `crypto/rand` for a spread | `internal/shared/telemetry.go:263-269` @ T2 |
| `stateFiles` is also the move-out-of-data_dir list | `internal/web/statedir.go:24-79` @ T2 |
| The core's file is `<data_dir>/feeds.json` | `interfaces.md` Task 3 (`Config.FeedsPath`), spec §2 |
| `SendCommand` refuses > `MaxMessageBytes` before dialling with `ErrRequestTooLarge` | `internal/shared/socketclient.go:20-31` @ T2 |
| A core refusal reaches the web as `core error: <text>` | `internal/web/client.go:450-466` @ T2 (`UpdateFeed`) |
| Matching `ErrApplyInProgressText` by substring is the existing pattern | `internal/web/handler_apply.go:287` @ T2 |
| Formats and counts of every catalogue body | research scratchpad `96bbc7d1-…/scratchpad/feeds2/` (downloaded 2026-09-24 17:00), read by a throwaway test — plan Step 8 |

### Decisions not in the spec

| # | Decision | Why |
|---|---|---|
| W-a | State file is `web/feed_fetch.json`, not `web/feeds.json` | `prepareStateDir` copies every `stateFiles` name from `data_dir` into `data_dir/web` and removes the original; `feeds.json` there is the core's. Where the web user can write data_dir (demo, single-user manual install) that would move the core's copy. **Deviation from interfaces.md and spec §2** — Task 8 should say `feed_fetch.json` where docs name the file |
| W-b | `feedRows(state *shared.RulesState)` | the handler already has `GET_RULES`; **deviation from interfaces.md** |
| W-c | gzip left to the transport | stdlib does it and caps correctly (Sources) |
| W-d | Next pull = last attempt + interval exactly, catalogue and own feeds alike; the offset places only the first pull of a feed with no attempt on record (first slot at or after the pass that first found it, remembered in memory); retry 1 h doubling to the interval; 429 → the interval | controller ruling after R3 #2: the first version aligned every pull to the offset slot, which pulled an hourly list up to 2 h apart and a daily own feed at 23 h 59 min (CrowdSec: 429) |
| W-e | P7 retry re-offers the parsed update from memory, no re-download | ET's hourly limit |
| W-f | Conditional GET only when the core holds a copy | P13 prunes copies; a 304 would otherwise point at nothing for ever |
| W-g | Spamhaus: both files or nothing; no validators | v6 is 5 % of the entries — under the shrink guard's 30 % |
| W-h | `LastAttempt` saved before the request | no restart-fetch loop |
| W-i | `refreshFeedNow` does nothing when the core already has a copy | a toggle off/on must not download |
| W-j | `saveOwnFeed` refused in demo mode | the demo is public |
| W-k | `refused` carries no core text into the web log | the core's audit entry has it; one rule — no response-derived text in the web log — is easier to hold than an exception |
| W-l | Plain format: one field per line | a hosts file is rejected, not read as 0.0.0.0 |
| W-m | The configuration.md table gains a sixth column | the existing table is transposed (one column per request); a row would not fit its shape |
| W-n | `notifications.md` and the `UpdateCheck` comment in `models.go` said "four requests" | found by grep; a fix carries its documentation |

| W-o | A switch-on never fetches inside a running backoff | R3 #3, X7 |
| W-p | Shrink refusal (`ErrFeedShrankText`) → reason `shrank`, `ShrankFrom` = core's `Entries`, `ShrankTo` = sent count; cleared on the next accepted update | ruling X1 |
| W-q | `http://localhost` no longer an own-feed host | R3 #8: `net/conf.go` hard-codes localhost only for NSS myhostname, so the name can go to DNS with Basic credentials in cleartext |
| W-r | A held update is dropped when an own feed's URL/user changes or the slot is cleared | R3 #10 |
| W-s | R3 #16 (spec's audit "rejected count") **not taken here**: the payload is T2's protocol and the audit entry T4's. The web knows the count (`feedFetchState.Rejected`); adding `Rejected int` to `UpdateFeedPayload` is a T2+T4 change, or the spec line becomes "dropped count" | ownership |
| W-t | A pass drops held updates of feeds no longer switched on; their rows show no error, but the store keeps the failure (commit `b9c6e75`, second on WEB) | Review Focus 1; the failure drives the backoff, so off → on after a 429 sends nothing until it has run (controller ruling on the first version, which forgot it) |

### Commands run, and results

| Command | Result |
|---|---|
| `go build ./... && go vet ./...`, and with `-tags integration` | clean |
| `go test -race ./internal/... ./cmd/...` | ok: core, shared, web (198 s), cmd/easywall-core |
| `~/go/bin/golangci-lint run --build-tags integration ./...` | `0 issues.` |
| `go test -c -tags integration ./internal/web` + `unshare -rn … -test.run Integration` | only `TestIntegration_ACertificateArrivesOverHTTP01` fails — at the base too (coordinator: podman) |
| `node scripts/prose-check.mjs` before/after | 1547 → 1549 sentences, 0 over 30, exit 0 |
| `codespell` on every changed file | exit 0 |
| Test files alone on `T2` (`$S/web-red`) | `go test ./internal/web`: 10 × `undefined` (first: `feeds_test.go:88:11: undefined: feedRunner`); the outbound guard: `internal/web/feeds.go … does not exist` |
| Throwaway test over the research's real bodies | table in plan Step 8; every count equals spec §1 |
| Mutation batches `$S/web-muts{1,2,3,4,5,6}.json` via `$S/web-mut.py` / `web-mut-w.py` in throwaway worktrees `$S/web-mut{,2,3,4}` (removed afterwards) | 76 mutations, every one red — plan Step 9. Two first went **NOTHING RED** and the tests were strengthened: M54b (the rows test had the stored status equal to the live one — `cins` now fails with a copy stored at the time and none now) and M53c (clearing a slot did not check its fetch state). Two first failed to compile (M30, M49b) and were rewritten. `git status --short` clean after each batch. Re-run after the R3 fixes and the scheduler ruling: `$S/web-muts{F1,F2,F3,G,H}.json` on `fb1155c`, 87 mutations, all red (the first pass of F used a -run filter without `Pull`, so M38/M39/M78 showed NOTHING RED until the filter was widened; M77 was rewritten to compile) |
| Plan re-applied to a fresh `e4f204b` worktree (`$S/web-apply`): every `Create` block written, every `diff` block through `git apply` | applies cleanly; `git diff --cached WEB` empty — the plan is the branch |

### Not verified

- **Against a real core.** `UPDATE_FEED`/`GET_FEEDS` are Task 4's; every test here uses the fake core. The seam: Task 4 must answer a refusal during an apply cycle with exactly `shared.ErrApplyInProgressText`, and `GET_FEEDS` must list every stored id with `Stored: true` — otherwise the web sends validators it should not, or none.
- **Against the real servers.** No request to any catalogue host was made; bodies are the research's downloads. Whether each host honours `If-None-Match` beyond GitHub raw is unmeasured — a host that does not answers 200, which is only a full download.
- Tor's poll limit: UNVERIFIED in the research (:62); scheduled hourly like the catalogue says.
- The docs tables were **not rendered** (no Jekyll build); the configuration table is now six columns wide. Task 8 renders the pages.
- `check:ui` not run: no template, class or locale changed.
- A web process crash mid-refresh was reasoned about (W-h, `TestTheAttemptIsRecordedBeforeTheRequest`), not reproduced.

## Task 7

## Task 7 — author notes (UI)

Branch `UI` in `$S/wt-UI`: three commits, head `f804bb5`, on `WEB` @ `fb1155c` (after the pre-flight fixes; first authored as `f1ba773` on `e810ed8`).

### Sources relied on

| Fact | Source |
|---|---|
| `feedRow`, `feedRows(*RulesState)`, `saveOwnFeed`, `refreshFeedNow`, the five statuses, eleven error reasons, `errOwnFeed*` | `internal/web/feeds.go:505-678`, `feed_state.go:20-58, 187-277` @ `e810ed8` |
| `refreshNow` asks `GET_FEEDS` before it fetches, and does nothing when the core holds a copy | `feeds.go:137-141, 230-243, 267-270` @ `e810ed8` — the basis of `TestSavingTheFeedsFetchesOnlyTheNewOnes` (no network) |
| The core validates `Rules.Feeds` (unknown, duplicate) | `internal/shared/validate.go:92-105` @ `e810ed8` |
| A form cannot nest, so the Feeds form needs the blocklist's form closed first | HTML Living Standard, `form` content model ("no form element descendants"); the old template wrapped the whole page grid in one form (`blocklist.html:16-76` @ `e810ed8`) |
| `.module` card, `:has(.toggle:checked)` edge, head as a `<label>`, disclosure outside it | DESIGN.md § Protection modules; `app.css:2349-2439` |
| Colour only for firewall state; a word beside every dot | DESIGN.md § Status, § Chips and tags |
| `.chip` is the data face; `.log-action` the UI face for language | `app.css:1397-1411, 3291-3304` |
| `TestEveryPageIsDocumented` reads only `Get`/`Handle` routes | `internal/shared/docs_coverage_test.go:83-88` — two POST routes need no entry |
| `CrossOriginProtection` covers every route in the router | `server.go:525-530` (`r.Use` before any group) |
| Gate walk count: 47 at `e810ed8`, 49 on `UI`, floor `41` | measured with a temporary `t.Logf` in `TestTheGateCannotBeWalkedPast`; its comment ("41") is stale **before** this branch |
| The locale file carries no plural forms | every entry is `{"id","translation"}` (`i18n_coverage_test.go:48-54`); `T` passes a map as template data only (`i18n.go:110-122`) |
| Tailwind emits `font-data`, `text-ink-muted` from `@theme` + template scan | `app.css:20-26, 50-88`; grep of the built sheet (Step 6) |
| `style.css` rebuilt from `e810ed8`'s `app.css` is byte-identical to the committed one | rebuilt in a throwaway worktree: `git status` clean |

### Decisions not in the spec

In the plan's decision table. The ones another task should know:

| # | Decision |
|---|---|
| U-a | `blockedRuleOption["feed"]` is `""` now (T2-d had `log_feed_connections`); the feed chip goes to `/blocklist#feed-<id>`. The guard's exception list gained `feed` |
| U-b | Demo: `spamhaus-drop`, `dshield`, `blocklist-de`, `et-compromised`, `cins` on in all three states (T2 had two). Staged = Current kept, because `TestDemoSend_AnOptionDriftIsPending` holds that the option drift is the only seeded pending change |
| U-c | `feedRow.ConfirmKey` added to T6's struct (one field, one `if` in `feedRows`) |
| U-d | No lockout check when staging a feed; Task 5's /apply verdict is the net |
| U-e | No filter by feed id on /blocked (YAGNI) |
| U-f | Screenshots of the demo show four of five statuses; *never fetched* needs a visitor's switch (Task 8 may want one shot after switching a feed on) |

### Commands run, and results

| Command | Result |
|---|---|
| `go build ./... && go vet ./... && go vet -tags integration ./...` | clean |
| `go test ./internal/... ./cmd/... -count=1` | ok: core, shared, web (78 s), cmd/easywall-core |
| `~/go/bin/golangci-lint run ./...` | `0 issues.` |
| `codespell` (repo config, and on every changed file but `de.json`/`style.css`) | exit 0 |
| test files alone on `e810ed8` + `go vet ./internal/web` | `undefined: stagedFeeds` (plan Step 2) |
| `$S/ui-mut.py` over `ui-muts{1,2,3,4}.json` in throwaway worktree `$S/ui-mut` (removed) | 34 mutations, all red after fixes — plan Step 10. First pass: M11 and M30 and M32 NOTHING RED → dead guard deleted, mutation re-aimed, test added |
| `check:ui` on `UI` (demo 127.0.0.1:12285, `flock $S/checkui.lock`) | `ok feeds card: 8 rows, 16 links with rel, …`, **UI checks passed** |
| `check:ui` with M1 (no ✗ confirmation) | `FAIL feeds card — a ✗ feed was staged without its confirmation box` |
| `node $S/ui-shots.mjs` (Playwright, 3 pages × 3 widths × 2 themes × 2 languages) | 36 shots, no sideways scroll, nothing clipped; three defects seen in the images and fixed (plan Step 9) |
| plan re-applied to a fresh `e810ed8` worktree (`Create`/`Replace` blocks written, `diff` blocks through `git apply`, `npm run build:css`) | `git diff --cached UI` empty — the plan is the branch |

Screenshots, `$S/ui-shots/`: `{blocklist,allowlist,blocked_rule_feed}-{en,de}-{dark,light}-{1600,900,390}.png` (36).

### Not verified

- **Chrome extension check** — the controller's. Playwright only.
- **Against a real core**: every test uses the fake core or the demo; `GET_FEEDS`/`UPDATE_FEED` are Task 4's. `AllowlistOverlap` and `Packets` on the card are whatever Task 4 fills.
- Task 5's `ReasonInFeed` wording on /apply is not rendered here.
- The German copy was written, not reviewed by a native speaker beyond me; *Fremdliste* for a feed (D11) is used throughout, *Core-Dienst* as `de.json` already does.
- `fr`: no new keys (fall back to `en`); the count for the PR is 71.
- The `TestTheGateCannotBeWalkedPast` comment says 41 routes; the walk finds 47 at the base and 49 here. Pre-existing, proven at `e810ed8`; not changed.

## Pre-flight review

Three reviewers read the assembled plan against the spec and the combined tree before any task was dispatched (`review-R1/R2/R3`, kept in the planning scratchpad). Found: **4 Critical, 11 Important, 25 Minor**; all fixed in the plan and its branches except R3 #16 (resolved by amending spec §3 step 9). The Criticals: /options could not be saved on any host upgraded from 2.22 (P24); committing the plan would have re-reddened P1's guard; a feed that shrank for a real reason stayed on its old copy for ever (P21, found by two reviewers). The fix rounds found two more defects, both fixed: the scheduler pulled an hourly feed up to ~2 h apart (P25), and the first fix for Review Focus 1 let off → on bypass the backoff.
