# 2.22 plan — the authors' notes

Evidence behind the tasks in [the plan](2026-09-23-2.22-it-is-yours-and-it-says-why-implementation.md): every file:line relied on, the commands run for the compile and mutation checks, decisions the specs did not make, and what could not be verified.

## plan-P1.md

## Author notes

### Sources relied on (`d9fdb02` unless noted)

| Fact | Where |
|---|---|
| step 1 hashes before any proof; answers built at :130-136; `HashPassword` at :162 | `internal/web/handler_firstrun.go:116-175` |
| a rejected submission is stashed in the signed cookie | `handler_firstrun.go:265-275`; `firstrunpending.go:25-31` |
| `newTOTPSecret` 20 bytes, `decodeTOTPSecret` normalises, `formatTOTPSecret` groups | `internal/web/totp.go:137-168` |
| `NewServer` builds the `Server` and `passkeyCount`; `/firstrun` routes only while `IsFirstRun` | `internal/web/server.go:252-369`, `:534-544` |
| `flashClass` defaults to `alert-crit` | `server.go:1588-1596` |
| `richText` renders backticks as `<code>` | `server.go:1146-1199` |
| JSON handler on stdout; flags | `cmd/easywall-web/main.go:17-44` |
| the demo seeds a password; ui-check skips the wizard then | `scripts/demo-server.sh:65-80`; `scripts/ui-check.mjs:233-237` |
| CI's first-run instance logs beside its config | `.github/workflows/test.yml:283-317` |
| wizard screenshots spawn with inherited stdio on 12229 | `scripts/ui-check.mjs:2097-2150` |
| every existing step-1 POST in tests | `grep -n '"/firstrun"' internal/web/*_test.go` (29 hits, 11 step-1 POSTs) |
| postinst starts the services and prints nothing | `debian/postinst:89-94` |
| template `password = ""` | `config/web.toml:101` |
| compose mount, healthcheck block | `docker-compose.yml:52-81` |
| ids unpinned; HEALTHCHECK; its comment | `Dockerfile:49-51`, `:91-157` |
| entrypoint seeds, repairs `/etc/easywall` only, `test -w` loop | `docker/entrypoint.sh:25-73` |
| supervisord passes stdout through raw | `docker/supervisord.conf:61-64` |
| job id `build-docker`, `load: true`, `Test[1]`, clean-up | `.github/workflows/build.yml:97-252` |
| empty env value ignored; stored = differs from default | `internal/shared/env.go:288-342` |
| `LocalAddrContextKey` is the conn's local address | Go `src/net/http/server.go:1933` @ `go1.27.1` |
| compose merges volumes by target | `compose-spec/compose-spec` `13-merge.md` @ `914ec15` |
| Compose 5.1.3 on `ubuntu-26.04` | `actions/runner-images` `images/ubuntu/Ubuntu2604-Readme.md:71` @ `90ebb6f` |
| `healthAllowed`, the empty-list test | `internal/web/handler_health.go:77-86`; `handler_health_test.go:185-199` |
| guard fragments | `internal/shared/healthcheck_definition_test.go:105-112` |

### Decisions not in the spec

- Token generated **and logged in `NewServer`**, not `Start` — one place, and testable without a listener.
- Message text: `first run: enter this setup token at /firstrun to create the account`, attribute `token`.
- Refused submissions keep the answers; the token is read with `PostFormValue` only.
- The help text carries both commands through `richText`; `.field-help code` added as a second selector on the existing `.switch-detail code` rule rather than a new rule.
- postinst prints its hint only while `password = ""`.
- build-deb asserts the journal line with `grep`, not `journalctl -g`.
- `-healthcheck` dials **from** the target address. Without it the own-address rule does not fire for loopback aliases; measured, see Task 4's decision table.
- The own-address rule does not apply to an empty `health_allow`.
- docker.md gets two Task-1/Task-3 edits (a table row; the mount callout and the migration). Task 5 owns the rest of that page and should build on them, not replace them.
- No Go guard for the compose mount or `.gitignore`: the CI step proves both by mutation.

### Commands run, and results

All in a `git archive HEAD` copy at `scratchpad/plan-P1/`.

| Command | Result |
|---|---|
| `go build ./...`, `go vet ./internal/web` | ok |
| `go test ./...` (all three tasks applied, plus a one-line stand-in for Task 2) | ok — cmd/easywall-core, internal/core, internal/shared, internal/web |
| `~/go/bin/golangci-lint run ./...` | `0 issues` (after one fix: `sessionValues` takes `*httptest.ResponseRecorder`, bodyclose) |
| `~/go/bin/gosec -quiet ./internal/web/...` | nothing on the new files |
| Task 1 mutations M1–M7 (script `scratchpad/p1mut.py`) | all RED |
| Task 4 unit mutations (script `scratchpad/p1mut4.py`) | all RED |
| `npm run build:css` + grep | `.switch-detail code,.field-help code{…}` present |
| `node --check scripts/ui-check.mjs` | ok |
| first-run instance on 12271, `curl -X POST` without token | `303 https://127.0.0.1:12271/firstrun` |
| `node scripts/ui-check.mjs --screenshots /firstrun` against it | account created from the token in `web.log`; six `firstrun*` figures written from the piped stdout; the figure shows the field and help rendering (inspected, light, 1600) |
| `podman build --format docker` + the CI step (`scratchpad/p1-compose-step-t4.sh`) | all six assertions ok; mutations in the tables above RED |
| `~/go/bin/actionlint .github/workflows/build.yml` | only `main`'s existing findings |

### Not verified

- The full `npm run check:ui` suite: not run, to avoid colliding with other authors on its fixed ports 12229–12233. Its wizard path (`setUpAccount` + token from the log) and the screenshot path were run.
- The CI step under rootful Docker on GitHub's runner: run only under rootless podman (with `:Z` binds and `podman unshare` in place of sudo/host networking). Reaching `172.30.99.10` from the runner assumes rootful Docker's usual host route to a bridge network.
- The Debian postinst line and the `build-deb` journal assertion: not executed.
- The uid/gid pin by mutation: alpine assigns 100/101 either way today.
- Chrome render at 1600/900/390 × both themes × en/de: the controller's.


## plan-P2.md

## Author notes

**Every file:line relied on** (v2.21.1 / current `docs/specs-2.22-2.23`
branch — the branch has made no code or docs/_docs changes yet, so these
line numbers are exact, not offset):

- `config/easywall.toml:46-49` — the `[docker]` block before the change.
- `config/embed.go:17-25` — `config.Core` is the embedded source of truth;
  `internal/core/writeconfig_embed_test.go:19-45` already proves it loads
  and validates, which is why Task 2's guard decodes the same bytes rather
  than reading the file off disk.
- `internal/shared/models.go:423-436` (`DockerConfig`), `:452-463`
  (`CoreConfig`) — the struct the guard test decodes into; no field change.
- `internal/shared/config_docker_test.go` (whole file) — existing pattern
  for a `DockerConfig`/`toml.Decode` test in package `shared`, which already
  imports `github.com/jp1337/easywall/config` from `internal/shared/env.go:10`
  (no import cycle: `config` imports only `embed`).
- `internal/shared/config_docs_test.go:104-147`
  (`TestShippedConfigsMatchTheStructsTheyConfigure`) and `:152-167`
  (`TestShippedCoreConfigUsesTheCurrentIPv6Key`) — the existing guard style
  this task's new test follows (decode the shipped default, assert a fact
  about it).
- `docs/_docs/features/docker.md:27-58` — the "Three things"-adjacent intro,
  the detection paragraph (X5, lines 29-31) and the "Four ways" table (Task
  2, lines 51-58).
- `docs/_docs/configuration.md:151-168` — the `[docker]` section as it read
  before this task.
- `docs/_docs/installation/docker.md` — read in full (244 lines): the "First
  run" table (27-36), the `./config` ownership callout (44-50, untouched by
  this task), "What must persist" (52-70, pre-existing from 2.21.1 Task 8,
  untouched), "Why host networking" (158-180), "Your own certificate"
  (187-207), "Updating" (209-216).
- `docs/_docs/installation/first-run.md` — read in full (161 lines): "If you
  lose the password" (122-132), "Behind a reverse proxy" (134-143), "When it
  does not work" (145-154).
- `docs/_docs/installation/reverse-proxy.md` — read in full (132 lines):
  "Passkeys do not work through this" (101-118), specifically 111-116.
- `docs/_docs/installation/requirements.md` — read in full (76 lines): the
  host table (14-22).
- `docs/_docs/features/health.md:117-122` — read to confirm the false
  "asks `/healthz` at the bound address" claim (item C) is **not** touched
  by this task; it is Task 4's line per the brief.
- `docs-tech/threat-model.md:130-156` — confirms `trusted_proxies` (2.13) is
  what supersedes first-run.md's H claim, and gives the exact CVE identifiers
  already in `reverse-proxy.md` for cross-reference (not repeated in the doc
  text, to keep it short).
- `internal/core/dockerreconcile.go:1-90` and `internal/core/firewall.go:189-
  190` (`reconcilePoll: 2 * time.Second`, `reconcileWait: 90 * time.Second`)
  — read in full for X5's exact bound (90 seconds, once, only if no bridge
  existed at boot).
- `docker-compose.yml` (whole file) and `Dockerfile` (whole file) — read in
  full to confirm F's and L's claims (`healthcheck:` block since 2.19; the
  `NET_ADMIN`/`SYS_MODULE` history already narrated in "Why host
  networking") and to confirm Task 5 introduces no snippet that now
  disagrees with either file.
- `docs-tech/local-review.md` (whole file) — "The documentation site"
  section for how `scripts/docs-build.sh` builds `docs/_site` locally, and
  the `docs/_site/docs/*` vs. top-level redirect-stub trap.
- Watchtower: `gh api repos/containrrr/watchtower` — confirmed `archived:
  true`, `pushed_at: 2025-12-17`; not re-fetched here since the research
  document already carries the verified result and Task 5 only rewords the
  documentation, it does not re-derive the fact.

**Decisions made that the spec/research did not pin down:**

- *Where "Reaching it from elsewhere" (I) lives.* Research says only
  "shared" between Debian and Docker, not which page. Placed it in
  `docker.md`, immediately after the ownership callout: that page already
  frames the whole first-run flow as "on the server, over SSH", so it is
  where an operator meets the problem, and it is squarely inside this task's
  Docker-docs scope. `first-run.md` was not touched for I, to avoid a third
  edit competing with G and H on that same page.
- *Where J's `tls.hostname` note lives in `docker.md`.* No table exists in
  "Your own certificate" to add a row to (unlike G, which the spec names
  explicitly as a table); used a `>` callout, matching that page's existing
  callout style (the `./config`-ownership note uses the same block-quote
  form).
- *The exact wording of L's `init` row split.* The research only says "split
  the init row into Debian/Docker"; wrote two rows under the same `init`
  label rather than one row with an inline distinction, to keep each fact
  short and parallel per the table-cell length rule this repository's own
  `check:prose` enforces.
- *G's troubleshooting-table fix scope.* Research cites `first-run.md:151`
  and `:153` as "systemd-only" as well as the `:124-129` recovery block; line
  151 as read today ("That is not a port number") carries no systemd
  reference, so I read this as an off-by-a-row citation in the research and
  fixed the two rows that actually do name one path only — `:152`
  (`systemctl status`) and `:153` (the Debian-only certificate link) — rather
  than the literal line numbers given. Said here rather than guessed
  silently.
- *Not touching `docker.md`'s `./config` references.* Confirmed by reading
  Task 3's fixed interface in the brief (mounts `./easywall-config`) and by
  grep of the current `docker.md`: the ownership callout, the environment-
  variables cross-reference sentence and the "What must persist" `docker
  run -v` examples all still say `./config`/`config/ssl` today. Left every
  one of them alone; Task 3 owns that rename, and Step 5/6/7 above anchor on
  text before or after those lines specifically so the two diffs cannot
  collide regardless of merge order.

**Commands run for verification, and their results:**

Go (Task 2), in a `git archive HEAD` scratch copy at
`/tmp/claude-1000/.../scratchpad/plan-P2/`:

```
$ go build ./...
$ go test ./internal/shared/ -run 'TestTheEmbeddedDefaultDetectsDockerBridges|TestShippedConfigsMatchTheStructsTheyConfigure|TestTheShippedSampleLeavesPublishedPortsOpen|TestEveryConfigKeyIsDocumented|TestShippedCoreConfigUsesTheCurrentIPv6Key' -v
--- PASS (all five)
$ go test ./internal/shared/... ./internal/core/... ./internal/web/...
ok  	github.com/jp1337/easywall/internal/shared	2.318s
ok  	github.com/jp1337/easywall/internal/core	4.345s
ok  	github.com/jp1337/easywall/internal/web	78.063s
```

Mutation (Task 2): edited `config/easywall.toml` back to
`enabled = false` in the scratch copy —

```
$ go test ./internal/shared/ -run TestTheEmbeddedDefaultDetectsDockerBridges -v
    config_docker_test.go:28: the embedded easywall.toml ships docker.enabled = false; a Docker host loses every container's network at the first apply (2.22 finding A)
--- FAIL
```

— then restored `true` and re-ran the full three-package suite above to
confirm the fix reproduces the passing state before continuing.

Docs (both tasks), same scratch copy, run from the real repo's script
against the scratch files as explicit targets:

```
$ node scripts/prose-check.mjs <six touched files, full paths>
6 pages · 395 prose sentences · average 14.7 words · 0 over 30 · 0 over 40
```
(First pass measured 5 sentences over 30 words — the ones listed in Step 9's
note above — each was split in two and the check re-run clean; the text
quoted in the task steps is already the passing, split version.)

```
$ podman run --rm -v "$PWD/docs:/srv:Z" -v easywall-docs-gems:/usr/local/bundle \
    -w /srv docker.io/library/ruby:4.0.6 \
    sh -c 'bundle install --quiet && JEKYLL_ENV=production bundle exec jekyll build'
Generating...
                    done in 1.059 seconds.
```
Then confirmed by grep of the built `docs/_site/docs/**/index.html`: every
new heading/callout/table text rendered (`Reaching it from elsewhere`,
`Passkeys need`, `Debian / systemd`, `nfnetlink_log`, the smart-quoted `This
host's containers`), every new link resolved to a real `href`
(`/docs/features/docker/`, `/docs/installation/reverse-proxy/`,
`/docs/installation/docker/#your-own-certificate`), and the only literal
`{{ }}` left in the output is the pre-existing `{% raw %}` blocks in
`docker.md`'s `docker inspect` examples — nothing from this task's edits
leaked unrendered Liquid. `docs/_site` was removed from the scratch copy
afterward.

**Could not verify:** the Chrome render check — neither task touches a
template, CSS or locale file, so there is nothing for that gate to check;
said explicitly in both tasks' steps rather than silently skipped.


## plan-P3.md

## Author notes

**Files read in full:** `web/templates/options.html`, `internal/web/handler_options.go`, `docs/_docs/features/filters.md`, `internal/web/i18n_coverage_test.go:1-520`, `internal/core/nftables.go` `Apply` (`:569-858`) and every module builder (`:1127-1331`, `:1372-1933`, `:2379-2598`, `:2764-2776`), `config/easywall.toml:46-133`, both specs, the research, the 2.21.1 plan's Task 1. Read in part: `DESIGN.md` §§ components `:520-600`, Protection modules `:1190-1215`, Toggles `:1320-1345`, Tooltips `:1427-1432`, Measure `:922-945`; `internal/web/server.go` `render` `:742-804`, `loadTemplates` `:899-913`, `richText` `:1167-1199`, `templateFuncs` `:1422-1693`; `web/src/app.css:870-900, 2315-2430, 2565-2640, 3280-3310, 1620-1640`; `scripts/ui-check.mjs:1-70, 920-970, 1640-1860, 1890-1998`; `scripts/demo-server.sh`; `scripts/prose-check.mjs:1-80`; `internal/web/testhelpers_test.go:252-330, 440-530`; `docs-tech/invariants.md:1-125`.

**Other file:line relied on:** `internal/shared/reach.go:66-90` (BogonRanges, 11); `web/templates/password.html:330-342` (absolute docs link precedent); `web/templates/blacklist.html:55-72` (aside-card markup, info icon); `web/templates/blocked.html:171` and `internal/web/handler_blocked_test.go:210` (the only `#logging` users — repo-wide grep excluding `_site`); `locales/*.json` opt block `en/de:429-456`, `fr:346-373`, `options_readonly` `en/de:186`, `fr:159`; `docs/_config.yml` `kramdown: input: GFM`; `docs/Gemfile.lock:87-90` (kramdown 2.5.2, kramdown-parser-gfm 1.1.0); `locales/status.json` (fr `reviewed: true`, so `TestTranslationsAreNotCopiedEnglish` applies to fr — renamed fr labels are French, unaffected).

**Upstream, at pinned tags, via `gh api … ?ref=`:** `torvalds/linux@v6.12` — `net/ipv4/ip_input.c:242-257`, `net/netfilter/nf_conntrack_proto.c:440-475, 580-625`, `net/netfilter/nft_ct.c:380, 505`, `net/ipv4/netfilter/nf_defrag_ipv4.c:62-105`, `net/ipv4/ip_fragment.c:450-462`, `net/netfilter/nf_conntrack_proto_tcp.c:200, 838-853, 963-987, 1180-1262`, `net/netfilter/nf_conntrack_core.c:2057-2076`. `kramdown/parser-gfm@REL_1_1_0` — `lib/kramdown/parser/gfm.rb:24-28, 60-118`.

**Measured:**
- Built-site ids: `grep -o '<h[1-6][^>]*id="[^"]*"' docs/_site/docs/features/filters/index.html` → `which-to-turn-on`, `attack-protection`, `what-the-bogon-filter-drops`, `traffic-filtering`, `logging` …; `docs/_site/docs/configuration/index.html` → `firewall--protection-modules` for `` ### `[firewall]` — Protection Modules ``. (The `_site` on disk predates this task's heading; the new id is computed by the pinned rule, not read.)
- Fragments (P3-1): `unshare -rn`, `lo` MTU 1280, `ping -c 3 -s 3000 127.0.0.1`: prerouting −500 counter 18, input 0 — also 0 with no `ct` expression in the table, which is what `ip_local_deliver` predicts.
- RSTs (P3-3): raw-socket RST and RST+ACK ×5 each to port 9: `ct state invalid … rst` 10, `new` 0, `established` 0.
- Accessible name: with the disclosure moved into the header `<label>`, Playwright's `ariaSnapshot()` of `#opt-bogon_filter` reads `checkbox "Bogon filter Drops private and reserved IPv4 sources. On a LAN, whitelist your own network first. What does this change?"`; clicking the summary did not change the switch (the first draft of the ui check asserted that and survived the mutation — the assertion was dropped and the Go-side M10 added instead).

**Compile, test, mutation — in `scratchpad/plan-P3/` (`git archive HEAD`):**
- `go build ./...` ok; `go vet ./internal/web` ok; `gofmt -l internal/web` empty.
- `go test ./internal/... -count=1` — all `ok` (web: 81 s).
- Step 3 state: the four new tests red as described, `TestGFMHeadingIDMatchesTheBuiltSite` green.
- M1–M10 each red with the line in the Step 10 table (output copied from the run), restored, green.
- `npm run build:css` → the seven rules grep'd out of `web/static/style.css` as listed in Step 11.
- `npm run check:prose` rc 0: 32 pages, 1483 sentences, average 14.5, 0 over 30 (base rc 0 too).
- `npm run check:ui` against a demo on 127.0.0.1:12272 (`EASYWALL_DEMO_DIR=$HOME/.local/share/easywall-demo-t6p3`): `UI checks passed`, 0 FAIL, including `ok   an open /options disclosure fits its card at 390px in German` and every `pages fit German`. CSS mutation (`dd{white-space:nowrap}`) → FAIL as in Step 13.
- `--screenshots options` wrote both PNGs (2550×2334); looked at the light one: which-card above, 14 cards, every summary on its card's bottom edge. A Playwright crop of the open bogon card at 390 px `de` read cleanly. **Neither is the Chrome check** — that stays with the controller.
- Not run: `golangci-lint` (Step 15 has it), `scripts/docs-build.sh` (no new id read from a real build).

**Decisions beyond the spec** are in the task's Decisions table. Two more:
- `filters.md` also gains one sentence under the per-source quote — *every module runs before the whitelist, so it applies to a whitelisted address* — because four cards say "Whitelisted addresses count too" and the linked page should say it where the reader lands (`Apply :738-790`; the page said "before the whitelist" at `:36-37` without the consequence).
- The Traffic-filtering note's old claim "These carry … IPv6 neighbour discovery" is removed: F3 shows ND survives multicast drop by chain order, so the sentence was wrong for multicast, and IPv6 has no broadcast.

**Coordination:** Task 8 (G2) is expected to state the IPv4/IPv6 ping asymmetry in `filters.md` *Always on* (`:46-57`); this task edits `:24`, `:92-99`, after `:106`, `:159-160`, `:178`, `:30` only. Task 8's chip links need `opt-<toml_key>`; the 14 keys are `optionHelps`' — it can use `optionHelpFor` to validate its map in a test.

**Could not verify:** DHCP clients other than systemd-networkd (spec F4 says unmeasured; the copy says so). Whether any operator bookmarks `/options#logging` (no in-repo reference). The prose-check's average is corpus-wide; the new sentences were not measured on their own.


## plan-P4.md

## Author notes

### Sources relied on (v2.21.1, this branch)

| Fact | file:line |
|---|---|
| chain order | `internal/core/nftables.go:706` loopback, `:712-718` IPv6 mode, `:720` established, `:721` ICMP, `:739-775` modules, `:778` Docker, `:783` blacklist, `:788` whitelist, `:804-815` ports (`FiltersHost`), `:828` custom, `:842` final log |
| IPv6 block/passthrough carry no log | `addFamilyVerdict`, `nftables.go:1009-1019` |
| ICMP accept lists | `nftables.go:1159` (v4), `:1187-1193` (v6 + RA/NA) |
| ICMP flood accepts nothing | `addICMPFloodProtection :1475-1513`, `addPerSourceRateLimit :1781-1832`, `Over: true` `:1821` |
| port rule matching | `portAcceptRules :2647-2714` (l4proto `:2656`, sources via `cidrMatch`, comments-only → no rule `:2697-2703`); ranges `buildPortExprs :2806`; `PortInRule` `reach.go:299` |
| scope | `PortRule.FiltersHost`, `models.go:888-890` |
| ICMP decode bound, fragments | `internal/core/packetlog.go:186-189`, `:123-126`, `:153-162` |
| spill replay | `packetlog.go:356-366` (Unmarshal per line), rewrite `:379` |
| demo shapes / seed / applied config | `democlient.go:21-30`, `:150-199`, `:293-304`, `:420-423` |
| handler, rows, current stubs | `handler_blocked.go:19-43`, `:151-161`, `:181-197`; `handler_blocked_test.go:24-39` stubs `GetAppliedConfig` as `Recorded:false`, so every existing test renders no reason |
| settings are not live | `daemon.go:696-705` (`CmdSaveSettings` writes config only) |
| GET costs | `rules.go:107-117`, `appliedconfig.go:29-42` |
| T and dict | `i18n.go:110-122`, `server.go:1572-1585` |
| rule cell, details, card fixes | `blocked.html:106-112`, `:153-164`; `app.css:3276-3321`, `:3572`, `:3583` |

### Decisions the spec did not make

The table at the top of Task 8, plus:

- **Details line** (`ICMP type/code`) is in Task 8, not Task 7, so Task 7 stays a no-UI task that needs no `check:ui`.
- **Two demo shapes added** (IPv4 ping, 8443 from outside its sources) so the public demo shows the two reasons that are not "closed". `TestDemoPacketSourcesAgreeWithTheDemoLists` stays green.
- **`ICMPv4Accepted`/`ICMPv6Accepted` exported from `shared`**, pinned by a core **test**; `addICMPRules` is untouched, to keep the spec's "single core change".

### For the controller

- **`id="logging"` collides with Task 6.** `options.html:244` puts `id="logging"` on the `log_blocked_connections` card, and `blocked.html:171` plus `TestBlocked_EmptyStates` (`handler_blocked_test.go:210`) link to `/options#logging`. If Task 6 gives that card `id="opt-log_blocked_connections"`, one of the two ids has to move to a wrapper, or the empty-state link has to change. Task 8 does not depend on either: `blockedRuleOption` names no log switch.
- **Finding, not in these tasks:** `shared.Reachable` step 9 (`reach.go:217-228`) loops over `r.TCP` without `FiltersHost()`. A `scope = "forwarded"` rule on the web port reads as *open* on `/apply`, while the input chain never opens it (`nftables.go:804-808`). That is a false "reachable" on the lockout screen. The fix is one `if !rule.FiltersHost() { continue }` and a table row in `reach_test.go`, to add to whichever task the controller picks (CLAUDE.md: fix findings in the same pass).
- **Demo realism, not changed:** the demo forwards 8080→80 (`democlient.go:201`), and it also logs default drops to 8080. On a real kernel prerouting redirects before input, so the log would show :80. This predates the branch and does not affect the reason logic.

### Compile and mutation checks run

Scratch tree: `git archive HEAD | tar -x -C …/scratchpad/plan-P4/`. The code above was applied exactly as written.

| Command | Result |
|---|---|
| `go build ./...` | ok |
| `go test ./internal/core/ -run 'TestDecode\|TestReplay\|TestRing\|TestPersist\|TestHook'` | ok |
| Task 7 mutations M1–M4 (table, Step 4) | each red: `TestDecode_ICMPRecordsTypeAndCode` ×3, `TestReplayReadsLinesWithoutTheICMPField` ×1; green on restore |
| demo mutations (remove `ICMP`, `Type: 0`) | `TestDemoICMPRowsCarryTheirType` red both; green on restore |
| `go test ./internal/shared/ -run DropReason` | ok; seven mutations (Step 3 table) each red, with the named cases in the output |
| `go test ./internal/core/ -run TestICMPAccepts` | ok; three mutations (core v4 +8, core v6 −136, shared v4 −12) each red |
| `go test ./internal/web/` (after `npm run build:css`) | ok (78 s). Before the rebuild: red on `pkt-rule`/`pkt-why` in `TestTemplateClassesExistInStylesheet`, as expected |
| web mutations W1–W8 (Step 10 table) | each red on the named test; green on restore. The first W8 attempt (`.ICMPX`) broke template execution and turned every test red, so it was replaced by deleting the line: only `TestBlockedDetailsNameTheICMPType` red |
| the Task 6 dependency | ran first without ids: `TestEveryBlockedRuleLeadsToItsOption` red for all eight modules. To check the rest, scratch-only stand-in ids were added to `options.html` (`id="opt-<toggle name>"` on each `.module`), then green |
| `go test ./...` | ok ×4 packages |
| `~/go/bin/golangci-lint run ./...` | 0 issues (after `gofmt -w` on the two test files; the snippets above are the gofmt'd form) |
| `npm run build:css` + grep | the five rules present (Step 7) |
| `npm run check:ui` against the scratch demo on 127.0.0.1:12273 (scratch stand-in ids for Task 6) | `ok   35 default-drop rows each say why; a module chip reaches its card`, `UI checks passed`, exit 0 — every width, both themes, German sweep included |
| check:ui mutation: `.pkt-why` span deleted, binary rebuilt, demo restarted | `FAIL blocked reasons — 35 of 35 default-drop rows give no reason`, `1 UI check(s) failed`; restored and rebuilt |
| `npm run check:prose` with Step 11's docs applied | `32 pages · 1478 prose sentences · average 14.5 words · 0 over 30` |

The demo was stopped by its PID. `pkill -f <config path>` killed the calling shell on the first attempt, because `-f` matched that shell's own command line (`docs-tech/local-review.md:63` warns about this).

### Not verified

- **Chrome render**: the controller does this.
- **Real NFLOG ICMP delivery** was not run. It needs sudo and the netns harness. The snaplen of 128 (`packetlog.go:32`) is far above the 2 bytes read, and the unit test covers the decoder.
- **Task 6's real anchor spelling.** The tests assume `id="opt-<toml_key>"` exactly as the brief fixes it.


## plan-P5.md

## Author notes

**Read in full:** `internal/core/nftables.go` (all 2948 lines), `internal/core/nftables_check.go`, `internal/core/nftables_log_test.go`, `internal/shared/packetlog.go`, `internal/shared/reach.go`, `internal/core/integration_test.go`, `docs/_docs/features/filters.md`, `docs/_diagrams/rule-order.mmd`, the brief, `plan-P3.md` (Task 6: findings, decisions, Steps 4, 5, 8, 10, 13–17, Author notes). **Read in part:** `docs-tech/invariants.md` (sections around `:140-150`, `:295-330`, `:500-528`, and a grep of every chain/order row), `nftables_integration_test.go:1-160, 210-300, 430-515, 715-830`, `nftables_semantics_test.go:48-200, 400-470, 600-700, 860-902`, `nftables_check_integration_test.go:80-191`, `nftables_forward_test.go:1-140` (the router harness), `reach_integration_test.go`, `nftables_reserved_id_test.go:1-60`, `core/packetlog.go:30-200`, `shared/validate.go:17-130, 240-270`, `shared/packetlog_test.go:140-175`, `plan-P4.md` (headers, Task 8 Decisions and Steps 2 and 4), `config/easywall.toml:46-133`, the schema's `firewall` properties, `.github/workflows/test.yml:141-151`, CLAUDE.md, the 2.21.1 plan's header.

**Upstream, pinned** (`gh api repos/torvalds/linux/contents/<path>?ref=v6.12`): `include/uapi/linux/netfilter_ipv4.h:31-45`, `netfilter_ipv6.h:35-48`; `net/ipv4/ip_input.c:242-258`; `net/ipv4/netfilter/nf_defrag_ipv4.c:75-106`; `net/netfilter/nft_chain_filter.c:195-217`; `net/netfilter/nft_fib.c:29-64`; `net/netfilter/nf_conntrack_proto_icmp.c:27-42`; `nf_conntrack_proto_icmpv6.c:31-47`; `nf_conntrack_core.c:1893-1911`; `nf_conntrack_proto_tcp.c:185-201, 838-853, 963-987, 1160-1320`. RFC 8900 (§3.7, §3.9, §5.1, §5.3, §6.3, §6.5) and RFC 8200 §4.5, from rfc-editor.org.

**Measured** (host kernel 7.2.3, `unshare -rn`):
- ICMP conntrack state: 20 pings over `lo` at 10 ms, both families → `ct state new` 1, `established` 19.
- Whole integration suite at `HEAD`, rootless: `ok` 64.9 s (baseline). With this task: `ok` 75.7 s.
- The new tests `-count=3`: 18/18 PASS.
- M1–M7 as in Step 6, each red with the quoted line, then restored and green.
- P5-1 as quoted.
- A veth drops a frame longer than the receiving end's MTU: with only `va` lowered to 1280 the control ping got 0 of 2 — hence both ends in the test.
- `lo` starts down in `TestMain`'s namespace (`ping 127.0.0.1` → `Network is unreachable`); the test brings it up and restores it.

**Compile and checks, in `scratchpad/plan-P5/src/` (`git archive HEAD`):** `go build ./...` ok; `go vet ./internal/core` ok; `gofmt -l internal/` empty; `go test ./internal/... -count=1` ok (web 71 s); `golangci-lint run ./internal/core/...` and `--build-tags integration` both `0 issues`; `npm run build:diagrams` → `rendered 9 diagram(s) x 2 themes`, `check:diagrams` → `all current`; `check:prose` exit 0, `0 over 30`, with Step 9's `filters.md` paragraphs in place; the diagram rasterised with ImageMagick and read (layout right; its renderer drops spaces between words, so that image is not the render check). Step 9's two scripts were run against fixtures built from `plan-P3.md`'s en/de blocks and its `filters.md` diff: 15 values per locale replaced, 9 `filters.md` replacements, all JSON valid.

**Not run:** `npm run check:ui` and the screenshots (they need Task 6's template, which is not in the tree); `scripts/docs-build.sh`; the Chrome render check (controller). Real NFLOG delivery of a `fragment` row from the prerouting hook — the log rule is the same `addFiltered` shape as every other module and `TestIntegration_ModuleLogging_EmitsOneLabelledRulePerModule` still finds its prefix, but no packet was sent through the group.

**Decisions beyond the brief** are in the task's Decisions table. One more: the fragment chain is created only while the switch is on, rather than always, so every host that has it off — the default — keeps a byte-identical table, and `TestIntegration_Apply_CreatesBaseChains` and the snapshot tests need no change.


## plan-T13-v2.md (replaces the first Task 13)

## Author notes

**Sources relied on (HEAD = v2.21.1 + specs, `15574b1`):**
- `internal/core/nftables.go`: `:629` (`ValidateRules` in `Apply`), `:752-753` (bogon exemptions = whitelist + Docker CIDRs), `:778-780` (Docker CIDRs → `addCIDRAccept`), `:1278-1320` (`addBogonFilter`), `:1323-1370` (`ipv4SourceMatch`), `:1951-2033` (`cidrMatch*`, the length check at `:2008`), `:2379-2415` (`addCIDRAccept`), `:2443-2490` (`addBlacklistRule`), `:2492-2549` (`addCIDRDrop`, whose only caller is `:2450`), `:2551-2557` (`addWhitelistRule` → `addCIDRAccept`), `:2685-2697` (source loop and WARN). `:95-176` (`adder`, `builtRecorder`, `ruleAdder`).
- `internal/core/docker.go:139-150` (`detectPublishedPorts`), `:270-277` (a 4-byte target, `Contains`).
- `internal/core/config.go:501-506`, `:521-526` (`checkNetworkLists` → `ValidateNetworkList`, which uses `net.ParseCIDR`, `validate.go:212`).
- `internal/shared/validate.go:75-89` (blacklist/whitelist validated untrimmed), `:152` (sources, trimmed), `:240-249`, `:251-275`.
- `internal/shared/reach.go:114-128` (`Reachable` unmaps `src`), `:260-291`. Every `InAnyEntry` caller passes an unmapped address: `clientaddr.go:75, :111`, `handler_health.go:85`, `handler_blocked.go:248, :318-325`.
- Test seams: `nftables_reserved_id_test.go:13-38` (`recordingConn`, `easywallInetTableForTest`, `inputChainForTest`); `integration_test.go` (TestMain re-execs in `CLONE_NEWNET`); `nftables_integration_test.go:23-150` (`newIntegrationManager`, `chainText`, `indexOfRule`); `rules.go:346` (`emptyState`); `docker_published_ports_integration_test.go:33-55` (`dockerNATFixture`, `skipOrFailUnprovable`).
- Plan: Task 11 `:4878`, `:4908`, `:5467-5530` (`cidrDropMatch`); Task 12 `:5922-6004`; Task 8 Files `:3657-3665`; header seam table `:70-86`.

**Decisions the controller's brief left open:**
- `ParseNetwork` parses with `net.ParseCIDR` and converts the result. Using `netip.ParsePrefix` would drop entries like `10.0.0.0/08`, which validation accepts (measured). M2 guards this choice.
- The shared tests go in `validate_test.go`, beside `ParseNetwork`, and not in `reach_test.go`, which Task 12 appends to.
- A Docker published-port integration case, because `docker.go:145` has no other test that can reach it.
- The `portAcceptRules` WARN stays in place for any future gap between the parsers. Only its comment changes.
- `::ffff:0:0/96` reads as `0.0.0.0/0`, which is what it names. In the blacklist it drops all IPv4, the same as `0.0.0.0/0` does today. That entry is EINVAL at HEAD, so no running install holds it.
- There is no operator-docs change: no page in `docs/_docs` mentions mapped addresses (grep for `ffff`/`mapped` finds only changelog text about Docker remapping). The 2.22 changelog line belongs to Task 9: "An IPv4-mapped network (`::ffff:10.0.0.0/104`) in the blacklist or whitelist no longer fails every apply, and in a port rule's sources it is enforced instead of skipped. It is written as the IPv4 network it names."

**Commands run** (scratch `plan-P7/`, `git archive HEAD`; go1.27.1):
- At HEAD, the Step 1 integration test under `unshare -rn` gave the 5 failures and the WARN exactly as quoted. The core unit test gave the 10 lines quoted in Step 3, and `go vet ./internal/shared` gave `undefined: ParseNetwork`.
- After the change: the targeted tests all PASS. `go test ./internal/... -count=1` gives `ok` ×3. `golangci-lint` gives `0 issues.` with and without `integration`. The full rootless integration suite gives `ok` (62.6 s). `gofmt -l` is empty.
- Mutations M1–M9 ran with a script, one at a time. Each went red with exactly the lines in Step 9, and the tree was restored after each.
- The Step 8 differential checked 193 693 non-mapped entries with 0 differences (the mapped entries skipped).
- Merge check: I cloned `preflight-B/src`, reverted its old T13 commit (`aaa107d`) and applied this diff with `git apply --3way`. `docker.go`, `reach.go`, `validate.go` and `validate_test.go` applied cleanly on top of T12, T11 and T8. `nftables.go` conflicted only at `cidrDropMatch`. After the resolution above, `go vet` is clean, `go test ./internal/...` gives `ok` ×3, and the full rootless integration suite gives `ok` (77.5 s).

**Cost, relevant to Pre-flight B #2:** `net.ParseCIDR` is slower than
`netip.ParsePrefix`. `InAnyEntry` over 10 000 `/24` entries with no match took
0.93 ms at HEAD and 1.76 ms after (about 1.9×, `-benchtime 300x -count 3`). If
Task 8 parses once per request, as #2 proposes, the cost is paid once and this
does not matter. If Task 8 stays per row, #2's measured 197 ms per poll at
N=10 000 roughly doubles.

**Not verified:** a mapped entry in Docker `custom_networks` going through a
full `Apply` with Docker enabled, which needs live bridge detection. The path
is `addCIDRAccept` (`:779`), which M5 covers at unit and integration level.
